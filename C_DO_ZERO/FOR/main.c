#include <stdio.h>
#include <stdlib.h>

int main()
{
    int i;

/** (inicialização), (condição),
(se verdadeira repete),
(depois atualiza)
compara,executa,atualiza
 */

for(i=1; i<=10; i++){
        printf("%d",i);
    }

    return 0;
}
